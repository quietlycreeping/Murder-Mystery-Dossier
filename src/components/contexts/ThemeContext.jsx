/*=========================================================
 Author:     J. Orlando
 Date:       June 2026
 Description: Theme Context to manage component colors. 
			File will be uploaded to set colors specific to the current murder mystery
==========================================================*/
/*import { createContext, useContext, useState } from 'react';

//====== User Context ===========================
export const ThemeContext = createContext();

export const ThemeContextProvider  = ({ children }) => {
  const [theme, setTheme]=createContext();
//====== Context Functions ===========================

const test = "test";

//====== Context Values ===========================
  const value = {test}  

  return (
    <ThemeContext.Provider value={value}>
      {children}
    </ThemeContext.Provider>
  );
}
export default ThemeContextProvider;*/